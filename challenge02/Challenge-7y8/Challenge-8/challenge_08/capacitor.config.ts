import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Challenge 8 - Mapa con sensores',
  webDir: 'dist'
};

export default config;
