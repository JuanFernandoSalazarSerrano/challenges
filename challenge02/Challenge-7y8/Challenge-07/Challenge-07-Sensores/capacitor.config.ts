import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.challenge07.sensores',
  appName: 'Challenge 07 - Sensores',
  webDir: 'dist'
};

export default config;
